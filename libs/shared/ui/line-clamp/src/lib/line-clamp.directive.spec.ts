import { LineClampDirective } from './line-clamp.directive';

describe('LineClampDirective', () => {
  it('should create an instance', () => {
    const directive = new LineClampDirective();
    expect(directive).toBeTruthy();
  });
});
