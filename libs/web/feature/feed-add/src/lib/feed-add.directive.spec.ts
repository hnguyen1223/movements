import { FeedAddDirective } from './feed-add.directive';

describe('FeedAddDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedAddDirective();
    expect(directive).toBeTruthy();
  });
});
